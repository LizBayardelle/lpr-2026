Password Reset
==============

Hi <%= @user.first_name.presence || "there" %>,

An administrator has sent you a password reset link. Visit the URL below to set a new password:

<%= @url %>

This link will expire in <%= distance_of_time_in_words(Devise.reset_password_within) %>.

If you didn't request this, you can safely ignore this email.

--
Linchpin Realty - Private Lending Solutions
